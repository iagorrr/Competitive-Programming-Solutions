#include <bits/stdc++.h>
using namespace std;

void run() {
  int n, m;
  cin >> n >> m;
  cout << (n % m ? "NO" : "YES") << '\n';
}

int32_t main() {
  ios_base::sync_with_stdio(0);
  cin.tie(0);
  int t;
  cin >> t;
  while (t--) {
    run();
  }
}

// AC, geometry
