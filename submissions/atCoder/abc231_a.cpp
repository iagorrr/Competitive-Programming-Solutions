#include <bits/stdc++.h>
using namespace std;

int32_t main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	using ld = long double;
	ld a;
	cin >> a;

	a = a / (ld)100;

	cout << fixed << setprecision(10) << a << '\n';
}


// AC, ad-hoc
